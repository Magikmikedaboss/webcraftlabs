/**
 * Minimal markdown header parsing placeholder.
 * TODO: implement parsing for:
 * title, date, summary, tags
 */
export function parseFrontmatter(raw: string) {
  return { meta: {}, body: raw };
}