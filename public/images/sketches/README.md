Sketches folder
================

Place visual sketch files here for reference in posts and simulations.

Naming convention:
- `sketch-<n>.<ext>` (e.g. `sketch-1.png`)

Example path (used by content): `/images/sketches/sketch-1.png`

To add the attached sketch image, save it as `sketch-1.png` in this folder.
